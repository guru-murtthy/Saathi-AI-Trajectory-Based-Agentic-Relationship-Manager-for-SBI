services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - SAATHI_LLM_PROVIDER=offline

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    environment:
      - VITE_API_URL=http://localhost:8000
    depends_on:
      - backend
